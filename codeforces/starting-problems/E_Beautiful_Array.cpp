#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'

void solve() {
    int a, b;
    cin >> a >> b;
    cout << 3 << endl;
    cout << b << " " << b << " " << (3*a)-(2*b);
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    solve();

    return 0;
}